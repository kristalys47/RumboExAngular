import { FinishedTaskPipe } from './finished-task.pipe';

describe('FinishedTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FinishedTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
